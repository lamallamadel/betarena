{
  "main": "lib/index.js",
  "scripts": {
    "lint": "eslint src",
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions --project crossteamz",
    "shell": "npm run build && firebase functions:shell  --project crossteamz",
    "start": "npm run shell",
    "deploy": "node ../scripts/validate-env.js --functions && firebase deploy --only functions --project crossteamz",
    "logs": "firebase functions:log  --project crossteamz",
    "validate:env": "node ../scripts/validate-env.js --functions"
  },
  "name": "functions",
  "engines": {
    "node": "22"
  },
  "dependencies": {
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^7.0.0",
    "funkora": "file:..",
    "ioredis": "^5.4.1"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.12.0",
    "@typescript-eslint/parser": "^5.12.0",
    "eslint": "^8.9.0",
    "eslint-config-google": "^0.14.0",
    "eslint-plugin-import": "^2.25.4",
    "firebase-functions-test": "^3.4.1",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  },
  "private": true
}